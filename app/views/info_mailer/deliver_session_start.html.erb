<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>Hallo Miro, Robert und Benedikt,</h1>
<%= @user.name %> ( <%= @user.email %>) beginnt nun eine Session der Arbeitsgruppe <%= @group_hour.id %> 
mit <%= @group_hour.users.count %> geplanten Benutzern:<br>
<br>
<ul>
<% @group_hour.user_hours.each do |user_hour| %>
<li><%= user_hour.user.name %> - eingeloggt: <%= if user_hour.login_count>0 then "ja" else "nein" end %></li>
<% end %>
</ul>
  </body>
</html>